#pragma once
#include "Sink.h"

// class LuckfoxCameraSink: public Sink
// {
// public:
//     static LuckfoxCameraSink* createNew(UsageEnvironment* env, int width = DISP_WIDTH, int height = DISP_HEIGHT);
//     LuckfoxCameraSink(UsageEnvironment* env, int width, int height);
//     virtual ~LuckfoxCameraSink(); 
//     virtual std::string getMediaDescription(uint16_t port);
//     virtual std::string getAttribute();
// protected:
//     virtual void sendFrame(MediaFrame* frame);
// private:
//     int mWidth;
//     int mHeight;
// };